[project]
name = "georeport-dr"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "asyncpg",
    "python-multipart",
]

[tool.vercel]
entrypoint = "impactmapper:app"
