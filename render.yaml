services:
  - type: web
    name: impactmapper
    runtime: python
    buildCommand: pip install fastapi uvicorn asyncpg python-multipart
    startCommand: uvicorn impactmapper:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        sync: false
